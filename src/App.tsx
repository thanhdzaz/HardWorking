/* eslint-disable no-unused-vars */
import { observer } from 'mobx-react';
import './App.css';
import Router from './components/Router';


const App = observer(()=>
{

  
    return (
        <Router />
    );
    
});
    

export default App;
